namespace CMS.Mcp.Client.Extensions
{
    using System.Threading.Tasks;
    using Contracts;
    using Microsoft.SemanticKernel;
    using ModelContextProtocol.Client;

    //public class McpClientPlugin(IMcpClientProvider mcpClientProvider)
    //{
    //    [KernelFunction]
    //    public async Task<string> CallToolAsync(string input) {
    //        var client = await mcpClientProvider.GetClientAsync();
                
    //           client.CallToolAsync(input);
    //        return result ?? "No response";
    //    }
    //}
}